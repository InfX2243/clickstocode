import { useState } from 'react';
import { ChevronDown } from 'lucide-react';
const faqs=[['Is this workshop 100% free to attend for students?','Yes, entirely free of cost! Organized as an educational community initiative by the AWS Student Builder Group MHSSCE.'],['Do I need prior AWS or advanced coding experience?','No prior expertise is needed. The sprint is designed from Level 100 up to Level 200, starting from the graphical AWS Management Console.'],['How does Systems Manager connect without Port 22?','AWS Systems Manager Agent communicates outbound over secure HTTPS (Port 443) to AWS management endpoints, removing the need for open inbound ports.'],['How do I register and guarantee a seat?','Click the RSVP on Meetup button and reserve your spot on the official AWS SBG MHSSCE Meetup event page. Physical seating is limited.']];
export default function FAQ(){const [open,setOpen]=useState<number|null>(null);return <section id="faq" className="w-full bg-[#0a0e18] py-24 relative border-b border-white/5"><div className="max-w-4xl mx-auto px-6 flex flex-col gap-10"><div className="text-center"><span className="text-xs font-mono text-[#00d26a] uppercase tracking-widest font-semibold">PRE-FLIGHT HUD CHECKLIST</span><h2 className="text-3xl sm:text-4xl font-extrabold text-white mt-2">Prerequisites &amp; Frequently Asked Questions</h2><p className="text-xs font-mono text-[#8e95a5] mt-2">Check your readiness before stepping into the seminar hall.</p></div><div className="grid grid-cols-2 sm:grid-cols-4 gap-3 p-4 rounded-2xl bg-[#1c1f2a] border border-white/10 text-xs font-mono">{['Laptop & Charger','Active AWS Account','Modern Web Browser','Zero Fees Required'].map(x=><div key={x} className="p-2 rounded-lg bg-[#0a0e18] text-white">✓ {x}</div>)}</div><div className="flex flex-col gap-3">{faqs.map(([q,a],i)=><div key={q} className="p-5 rounded-2xl bg-[#1c1f2a] border border-white/10 hover:border-[#00d26a]/40 transition-all"><button className="w-full flex items-center justify-between text-left" onClick={()=>setOpen(open===i?null:i)} aria-expanded={open===i}><span className="text-sm font-bold text-white">{q}</span><ChevronDown className={`text-[#00d26a] transition-transform ${open===i?'rotate-180':''}`}/></button>{open===i&&<p className="text-xs text-[#8e95a5] pt-3 leading-relaxed">{a}</p>}</div>)}</div></div></section>}
